# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

This repository is a fresh scaffold with no source code yet — only a README describing the intended purpose: a custom console for F5 BIG-IP DNS. There is no build system, package manifest, test suite, or architecture to document.

When code is added, update this file with actual build/lint/test commands and real architectural notes rather than generic placeholders.
